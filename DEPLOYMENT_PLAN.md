# ComfyUI 单卡验证与镜像交付计划

## 范围与顺序

1. 审计 `user/default/workflows_api` 的 18 份已导出工作流，使用同一套可重复执行的串行测试程序。
2. 本机 HTTPS 逐个执行：保留模型和图结构，用上传接口提供小尺寸输入，限制采样步数及文本长度；每项保存实际请求、历史、输出与失败原因，完成后卸载模型。测试不代表原始大分辨率下的性能或画质验收。
3. 修复实测暴露的依赖、输入或模型路径问题；无法取得的模型、缺失组件和资源限制如实记录，不能以节点注册成功替代推理成功。
4. 编写项目内 Dockerfile、构建/运行配置及脚本。只构建一个 ComfyUI 业务镜像，模型从外部只读挂载，输入、输出、用户数据独立持久化。与 rail-system Compose 的构建入口保持一致。
5. 本机任务结束后释放 GPU，启动 GPU 容器。从容器外执行同一批工作流，验证容器实际使用 NVIDIA GPU、模型挂载、结果可下载。
6. 记录构建参数、镜像标识、硬件信息和测试限制。分项英文 commit，维护中文 `commitlog.md`，检查后推送当前远程分支。

## 硬件边界

当前实测机器为 Ubuntu、RTX 3090 24 GB、驱动 595.91.07。沿用已验证的 Python 3.12 / PyTorch CUDA 13.0 软件栈。4090 属于后续同镜像兼容目标，不声称已经实机验证；GPU 选择、显存策略和端口采用运行时配置，CUDA/PyTorch 版本属于构建配置。

## 提交划分

- 固定 Python 环境与 uv 安装流程。
- 迁移模型目录为整体相对链接。
- HTTPS 启停与就绪检查。
- 串行工作流验证工具及实测修复（按独立问题进一步拆分）。
- GPU Docker 镜像、运行配置及交付文档。
- 最终测试结果与提交索引。

不提交模型、虚拟环境、证书私钥、运行缓存或测试图片；已有无关 Manager 缓存修改保留在工作区。
